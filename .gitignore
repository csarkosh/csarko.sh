.DS_Store

# Firebase CLI
.firebase/
firebase-debug.log*

# Terraform
_infra/.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
!_infra/terraform.tfvars.example

# Python
__pycache__/

# Credentials. Keys live outside the repo (~/.config/<project>/gsc-sa.json);
# these patterns are a second line of defence, since this repo is public.
*-sa.json
*service-account*.json

# Agent worktrees and scratch
.worktrees/
.superpowers/

# The substack-post voice profile lives in ~/.config/csarko-sh/, never here.
voice.md
